name: Lint

on:
  pull_request:
  workflow_dispatch:

jobs:
  lint-language:
    name: Lint language
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6
      - name: Run woke
        uses: get-woke/woke-action@v0
        with:
          fail-on-error: true
